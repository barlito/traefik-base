server:
  address: 'tcp://0.0.0.0:9091/'

log:
  level: info

authentication_backend:
  file:
    path: /config/users.yml

session:
  cookies:
    - domain: 'barlito.fr'
      authelia_url: 'https://auth.barlito.fr'
      default_redirection_url: 'https://traefik.barlito.fr'

storage:
  local:
    path: /data/db.sqlite3

access_control:
  default_policy: deny
  rules:
    - domain: '*.barlito.fr'
      policy: two_factor

notifier:
  filesystem:
    filename: /data/notification.txt

totp:
  issuer: 'barlito.fr'
